{
   "name": "Dank Farmer",
   "repository": "https://github.com/en-25/dank-farmer",
   "stack": "heroku-20",
   "env": {
      "TOKEN": {
         "description": "Your account's token, used for grinding dank memer.",
         "required": true
      },
      "PREFIX": {
         "description": "Prefix used to run commands",
         "required": true
      },
      "CHANNELID": {
         "description": "Enter the channelid in which you wants to grind the Dank Memer.",
         "required": true
      }
   }
}
